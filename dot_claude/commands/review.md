Review the staged changes (or specified files/diff) for:

1. **Security issues** - injection, XSS, secrets exposure, auth bypasses
2. **Runtime safety** - nil/null dereferences, race conditions, resource leaks
3. **Code correctness** - logic errors, edge cases, error handling
4. **Language idiomaticity** - Go/Rust/Python conventions and best practices
5. **Performance** - unnecessary allocations, inefficient algorithms, hot path issues
6. **Test coverage** - missing tests, untested edge cases

Be thorough and pedantic. Present issues interactively, ordered by severity.
For each issue, offer to make the fix.
