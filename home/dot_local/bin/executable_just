#!/usr/bin/env bash
set -eo pipefail

_just() {
  local version="1.13.0"
  local just="$HOME/.cargo/bin/just"
  if [ ! -x "$just" ] || [[ "$("$just" --version)" != *"$version"* ]]; then
    cargo install --version "$version" just
  fi

  exec "$just" "$@"
}

_just "$@"
