#!/usr/bin/env bash
set -eo pipefail

_fn() {
  local version="1.20.3"
  local prog="$HOME/.local/go/bin/go"
  if [ ! -x "$prog" ] || [[ "$("$prog" version)" != *"$version"* ]]; then
    mw install go
  fi

  exec "$prog" "$@"
}

_fn "$@"
