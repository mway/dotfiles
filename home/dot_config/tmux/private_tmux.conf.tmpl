# ============================================================================
# Prefix
# ============================================================================
# Use C-b as prefix (default)
set-option -g prefix C-b
bind-key C-b send-prefix

# ============================================================================
# Pane Navigation (Alt+Arrow keys)
# ============================================================================
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Also support vim-style navigation with Alt+hjkl
bind -n M-h select-pane -L
bind -n M-l select-pane -R
bind -n M-k select-pane -U
bind -n M-j select-pane -D

# ============================================================================
# Window (Tab) Navigation
# ============================================================================
bind -n M-[ previous-window
bind -n M-] next-window

# ============================================================================
# Pane Splitting
# ============================================================================
# C-b _ splits vertically (new pane below)
bind _ split-window -v -c "#{pane_current_path}"

# C-b | splits horizontally (new pane to the right)
bind | split-window -h -c "#{pane_current_path}"

# Unbind default split keys
unbind '"'
unbind %

# ============================================================================
# Predefined Layouts
# ============================================================================
# C-b L 1: Default layout (fixed 125-col left pane, two variable-width right panes stacked)
bind -T layout 1 run-shell '\
    tmux split-window -h -c "#{pane_current_path}" \; \
    resize-pane -t 1 -x 125 \; \
    select-pane -t 2 \; \
    split-window -v -c "#{pane_current_path}" \; \
    select-pane -t 1'

# C-b L 2: Empty layout (single pane)
bind -T layout 2 kill-pane -a

# C-b L h: Reset to even horizontal layout
bind -T layout h select-layout even-horizontal

# C-b L v: Reset to even vertical layout
bind -T layout v select-layout even-vertical

# C-b L: Enter layout mode
bind L switch-client -T layout

# C-b R: Reset current layout (restore pane 1 to 125 cols, split right panes evenly)
bind R run-shell '\
    tmux resize-pane -t 1 -x 125 2>/dev/null; \
    h=$(tmux display-message -p "#{window_height}"); \
    tmux resize-pane -t 2 -y $((h/2)) 2>/dev/null || true'

# ============================================================================
# Quality of Life Improvements
# ============================================================================
# Enable mouse support
set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Renumber windows when one is closed
set -g renumber-windows on

# Increase scrollback buffer size
set -g history-limit 100000

# Enable 256 color support
set -g default-terminal "screen-256color"

# Faster command sequences (shorter escape time)
set -s escape-time 0

# Increase repeat time for repeatable commands
set -g repeat-time 1000

# Enable activity alerts
setw -g monitor-activity on
set -g visual-activity off

# Set terminal window title to session name
set -g set-titles on
set -g set-titles-string "#S"

# ============================================================================
# Pane Management
# ============================================================================
# C-b x: Close current pane (with confirmation)
bind x confirm-before -p "kill-pane #P? (y/n)" kill-pane

# C-b X: Close current window (with confirmation)
bind X confirm-before -p "kill-window #W? (y/n)" kill-window

# C-b z: Toggle pane zoom
bind z resize-pane -Z

# ============================================================================
# Pane Resizing
# ============================================================================
# C-b Alt+Arrow: Resize panes
bind -r M-Up resize-pane -U 5
bind -r M-Down resize-pane -D 5
bind -r M-Left resize-pane -L 5
bind -r M-Right resize-pane -R 5

# C-b Alt+hjkl: Resize panes (vim-style)
bind -r M-k resize-pane -U 5
bind -r M-j resize-pane -D 5
bind -r M-h resize-pane -L 5
bind -r M-l resize-pane -R 5

# ============================================================================
# Window Management
# ============================================================================
# C-b c: Create new window
bind c new-window -c "#{pane_current_path}"

# C-b ,: Rename current window
bind , command-prompt -I "#W" "rename-window '%%'"

# ============================================================================
# Session Management
# ============================================================================
# C-b d: Detach from session
bind d detach-client

# C-b r: Reload tmux config
bind r source-file ~/.config/tmux/tmux.conf \; display "Config reloaded!"

# C-b S: Toggle synchronize-panes (send keystrokes to all panes)
bind S setw synchronize-panes \; display "Synchronize-panes #{?pane_synchronized,ON,OFF}"

# ============================================================================
# Copy Mode (Vi-style)
# ============================================================================
setw -g mode-keys vi

# C-b [: Enter copy mode
bind [ copy-mode

# C-b ]: Paste buffer
bind ] paste-buffer

# In copy mode, v begins selection and y copies
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-selection-and-cancel
bind -T copy-mode-vi C-v send-keys -X rectangle-toggle

# Mouse selection automatically copies to system clipboard (macOS)
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"
bind -T copy-mode MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel "pbcopy"

# ============================================================================
# Plugins
# ============================================================================
# tmux-resurrect: Save and restore tmux sessions
run-shell ~/.config/tmux/plugins/tmux-resurrect/resurrect.tmux

# Resurrect settings
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-processes 'hx claude-resume codex-resume'
set -g @resurrect-dir '~/.config/tmux/resurrect'

# ============================================================================
# Theme
# ============================================================================
source-file ~/.config/tmux/dracula.conf
