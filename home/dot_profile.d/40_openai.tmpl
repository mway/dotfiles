#!/usr/bin/env bash
# Managed by Chezmoi

unset CODEX_HOME

codex() {
  command codex \
    --sandbox=danger-full-access \
    --ask-for-approval=on-request \
    --search \
    --cd "${PWD}" "$@"
}

codex-resume() {
  codex resume --last
}
