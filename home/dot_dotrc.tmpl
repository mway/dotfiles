# Managed by chezmoi

function maybe_source() {
    # shellcheck disable=SC1090
    [ -z "$1" ] || [ ! -f "$1" ] || source "$1"
}

export PATH="{{ .path.path }}"

maybe_source "$HOME/.local/etc/homebrew/env"
