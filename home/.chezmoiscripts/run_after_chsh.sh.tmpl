#!/usr/bin/env bash

source "${HOME}/.profile.d/00_core"
ansi-badge-blue "  chsh" "Setting shell to $(ansi --bold {{ quote .shell }})"
sudo chsh -s {{ quote .shell }} {{ trim (output "whoami") }} >/dev/null 2>&1
