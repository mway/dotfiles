#!/usr/bin/env bash
# global cache {{ .cache.global }}

source "$(chezmoi source-path)/scripts/utils.sh"

_crate_install() {
  source "$HOME/.cargo/env"

  {{- range $crate := .crates }}
    cargo install -q --color never {{ $crate }}
  {{- end }}
}

_manage "Cargo crates" _crate_install
