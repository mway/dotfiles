#!/usr/bin/env bash

[[ ! -f ~/.bashrc ]] || source ~/.bashrc
[[ ! -f ~/.dotrc ]] || source ~/.dotrc

{{ range (onepassword "home.mway.dev").fields -}}
{{   if eq .label "api.homebrew.token" -}}
export HOMEBREW_GITHUB_API_TOKEN="{{ .value }}"
{{   end -}}
{{ end -}}
