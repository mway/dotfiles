# Ignore everything by default and whitelist things explicitly.
/*

# XDG home
!/.config

# Always ignore
.DS_Store
.*.sw?

# Useful scripts/tools are placed in ~/bin
!/bin

# shell
!.aliases
!.bashrc
!.bash_profile
!.profile
!.profile.d/

# Misc
!.dotrc
!.envrc
!.envrc.local
!Brewfile

# git
!.gitconfig
!/.gitconfig.d/
/.gitconfig.d/local

# tig
!.tigrc

# tmux
!.tmux*.conf
!/.tmux/

# neovim
!.vimrc
!.config/nvim/
.config/nvim/autoload/plug.vim # managed by chezmoi
.config/nvim/plugged/ # managed by vim-plug
.config/nvim/plugged/* # managed by vim-plug
.config/nvim/plugged/**/* # managed by vim-plug

