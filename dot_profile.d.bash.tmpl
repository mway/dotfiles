#!/usr/bin/env zsh

find '{{ joinPath .path.home ".profile.d" }}' -type f -name "*.bash" -o \! \( -name "*.*" \) |\
    sort |\
    while IFS= read -r f; do
        # shellcheck disable=SC1090
        source "$f" || echo "[{{ joinPath .path.home ".dotrc.bash" }}] Failed to source $f" >&2
    done
